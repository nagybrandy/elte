First Steps
Installation and initial setup. For this, you will need an installed Node.js environment.

Create a React application using Vite. Use TypeScript.
After installation, start it (npm run dev).
Analyze the generated directory structure and the contents of the files.
Make simple changes to the files and observe their effects.
In the web developer toolbar, observe how the DOM updates when the page changes.