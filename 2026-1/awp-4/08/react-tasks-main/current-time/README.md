# Current Time

Create a component that displays the current time like this: "The current local time is: #time#".

1. Render this component.
2. Repeat the `ReactDOM.render()` call every second using `setInterval()`.
3. Open the browser developer tools and analyze what changes happen in the DOM.
