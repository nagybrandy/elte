# Random Background Color

Create a component that sets the background color of a `div` to some random value.

1. Render this component.
2. Put the `ReactDOM.render()` call inside a repeating timer.
3. Open the browser developer tools and analyze what changes happen in the DOM.